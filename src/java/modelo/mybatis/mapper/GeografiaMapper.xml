<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="geografia">
    <select id="coloniasPorCP" parameterType="int" resultType="hashmap">
        SELECT
        c.id            AS idColonia,
        c.nombre        AS nombreColonia,
        c.codigo_postal AS codigoPostal,
        m.nombre        AS nombreMunicipio,
        e.nombre        AS nombreEstado
        FROM colonias c
        JOIN municipios m ON m.id = c.municipio
        JOIN estados e    ON e.id = m.estado
        WHERE c.codigo_postal = #{value}
    </select>
</mapper>

